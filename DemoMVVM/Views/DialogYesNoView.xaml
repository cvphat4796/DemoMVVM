<Window x:Class="DemoMVVM.Views.DialogYesNoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DemoMVVM.Views"
        mc:Ignorable="d"
        Title="{Binding Path=Title}" Height="150" Width="300">
    <Grid>
        <TextBlock Height="70" Text="{Binding Path=Message}" ></TextBlock>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button x:Name="Ok" Content="OK" Margin="0 0 5 5"></Button>

            <Button x:Name="Cancel" Content="Cancel" Margin="0 0 5 5"></Button>
        </StackPanel>
    </Grid>
</Window>
